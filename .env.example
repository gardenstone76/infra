# MySQL root 비밀번호
DB_ROOT_PASSWORD=changeme

# Cloudflare R2 public URL — CampPick2 (Caddy /storage/* 프록시)
# 형식: https://pub-{hash}.r2.dev
CAMPPICK_R2_PUBLIC_URL=

# Cloudflare R2 public URL — Openfolio (Caddy /storage/* 프록시)
# 형식: https://pub-{hash}.r2.dev
OPENFOLIO_R2_PUBLIC_URL=
